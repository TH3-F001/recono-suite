pip install -r requirements.txt

#install subdomainizer and link as subdomainizer (through git)
#Install assetfinder using go install (download go if needed) go install  github.com/tomnomnom/assetfinder@latest
#install subscraper from github, link, etc (has install script)
# install github-subdomains go install github.com/gwen001/github-subdomains@latest
#install bbot pipx install bbot